<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_screen_dark"
    android:orientation="vertical"
    android:paddingLeft="20dp"
    android:paddingTop="34dp"
    android:paddingRight="20dp"
    android:paddingBottom="20dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:maxLines="1"
        android:text="@string/contacts_title"
        android:textColor="@color/textPrimaryDark"
        android:textSize="28sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="20dp"
        android:includeFontPadding="false"
        android:text="@string/contacts_subtitle"
        android:textColor="@color/textSecondaryDark"
        android:textSize="13sp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp"
        android:includeFontPadding="false"
        android:text="@string/contacts_available_devices"
        android:textColor="@color/textPrimaryDark"
        android:textSize="15sp"
        android:textStyle="bold" />


    <ListView
        android:id="@+id/telalistacontatos__lv_contatos"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:cacheColorHint="@android:color/transparent"
        android:divider="@android:color/transparent"
        android:dividerHeight="10dp"
        android:listSelector="@android:color/transparent"
        android:scrollbarStyle="outsideOverlay" />

    <TextView
        android:id="@+id/telalistacontatos_tv_vazio"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="18dp"
        android:background="@drawable/bg_contact_card"
        android:gravity="center"
        android:padding="22dp"
        android:text="@string/contacts_empty_message"
        android:textColor="@color/textSecondaryDark"
        android:textSize="14sp" />

</LinearLayout>
